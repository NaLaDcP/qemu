# Répertoire sur l'hôte contenant le fichier image kernel
IMAGE_DIR=/chemin/vers/ton/build

# Nom du fichier image dans ce répertoire
IMAGE_FILENAME=Image

# Fichier de noms de pins (optionnel) — copier pins.json.example et adapter
# PINS_FILE=/chemin/vers/mon/pins.json
